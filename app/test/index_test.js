describe('Unit: Skilltree', function() {

  var scope, element, controller, $window;

  beforeEach(function() {
        //module('Skilltree');    
  });

  // beforeEach(inject(function($rootScope, $controller, $cookies) {
  //   $cookies.rootRef = "https://Skilltree-test.firebaseio.com/";

  //   scope = $rootScope;
  // }));

  it('scope should be available, but scope.user should not be defined', function() {
    expect(true).toBeDefined();
  });
});
